{
  "name": "affiliate-tracker",
  "version": "1.0.0",
  "description": "This project sets-up an API that parses the query-parameters when invoked via a user clicking on a link",
  "main": "src/edge.ts",
  "scripts": {
    "build": "tsc",
    "prebuild": "env-cmd -f .env kysely-codegen --exclude-pattern spatial_ref_sys"
  },
  "author": "Lucas Raza",
  "license": "ISC",
  "dependencies": {
    "@vercel/node": "^3.0.5",
    "@vercel/postgres-kysely": "^0.4.1",
    "axios": "^1.5.0",
    "env-cmd": "^10.1.0",
    "kysely": "^0.26.3",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/pg": "^8.6.6",
    "kysely-codegen": "^0.10.1",
    "typescript": "^5.2.2"
  }
}
