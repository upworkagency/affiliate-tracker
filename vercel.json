{
    "builds": [
      {
        "src": "/api/edge.ts",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/",
        "dest": "/api/edge.ts"
      }
    ],
    "env": {
      "DATABASE_URL": "@database_url",
      "BEARER_TOKEN": "@bearer_token"
    }
}